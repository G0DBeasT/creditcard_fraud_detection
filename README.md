# creditcard_fraud_detection
A machine learning pipeline designed to detect fraudulent credit card transactions within a highly imbalanced dataset. This project implements a Random Forest classifier utilizing specialized techniques   like   │ class-weight balancing and hyperparameter tuning optimized specifically for the Average Precision Score (AUPRC).
